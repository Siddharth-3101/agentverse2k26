import { NavLink } from 'react-router-dom';
import { LayoutDashboard, Flame, CalendarDays, Users, Award, Trophy, FileText, BriefcaseBusiness, UserRound, BrainCircuit, Settings, X } from 'lucide-react';
const primary = [['Dashboard', 'dashboard', LayoutDashboard], ['Opportunities', 'opportunities', Flame], ['Events', 'events', CalendarDays], ['Clubs', 'clubs', Users], ['Certifications', 'certifications', Award], ['Achievements', 'achievements', Trophy], ['Publications', 'publications', FileText], ['Internships', 'internships', BriefcaseBusiness]];
const lower = [['My Portfolio', 'portfolio', UserRound], ['Skill Graph', 'skills', BrainCircuit]];
export default function Sidebar({ open, onClose }) { const links = (items) => items.map(([text, to, Icon]) => <NavLink onClick={onClose} key={to} to={`/student/${to}`} className="nav-item"><Icon size={19}/><span>{text}</span></NavLink>); return <aside className={`sidebar ${open ? 'open' : ''}`}><div className="brand"><span className="brand-mark">C</span><span>Campus<span>Verse</span></span><button className="close-nav" onClick={onClose}><X size={20}/></button></div><nav>{links(primary)}<div className="nav-divider" />{links(lower)}<div className="nav-divider" /><a className="nav-item" href="#settings"><Settings size={19}/><span>Settings</span></a></nav><div className="sidebar-promo"><span className="promo-icon">✦</span><b>Shape your next step</b><small>Add a project to boost your profile.</small><button>Update profile</button></div></aside> }
